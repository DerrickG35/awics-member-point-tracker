<% @current_user = Admin.where(id: 1) %>
<% @current_user.each do |user| %>
  <% @userList = User.where(:email => user.email) %>
  <% @userList.each do |match|%>
    <% if !match.email.blank? %>
      <h4>Welcome: <%= user.full_name %> - <%= user.email %> (<%= match.position_id %>) <h4>
    <% end %>
  <% end %>
<% end %>

<% if !flash[:notice].blank? %>
  <div class="notice">
    <%= flash[:notice] %>
  </div>
<% end %>

<div class="users index">

  <h1>Members</h1>

  <table class="table_center" summary="User list" cellspacing="10" style="width:700px">
    <tr>
      <th>NetID</th>
      <th>Email</th>
      <th>First Name</th>
      <th>Last Name</th>
      <th>Position</th>
      <th>Points</th>
    </tr>
    <% @users.each do |user| %>
    <tr  style="text-align:left">
      <td><%= user.username %></td>
      <td><%= user.email %></td>
      <td><%= user.first_name %></td>
      <td><%= user.last_name %></td>
      <td><%= user.position_id %></td>
      <td><%= user.member_points %></td>

      <% @userList.each do |match|%>
      <td class="actions">
          <%= link_to("More Info", attendance_path(user), :class => 'action show') %>
      </td>
      <% if match.position_id == 'admin' %>
        <td class="actions">
          <%= link_to("Update Points", edit_user_path(user), :class => 'action edit') %>
        </td>
      <% end %>
      <% end %>
    </tr>
    <% end %>
  </table>
