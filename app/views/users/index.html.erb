<%# <h1>Aggie Women in Computer Science</h1> %>
<h3 class='centerText'>Dashboard</h3>
      <h4 class='centerText'>Current Login: </h4>

<div class='currentLoginCard'>
  <% @current_user = Admin.where(id: 1) %>
  <% @current_user.each do |user| %>
    <div class="avatarContainer">
      <%= image_tag(user.avatar_url, :class => "userAvatar")   %>
    </div>
      <p> <%= user.full_name %> - <%= user.email %></p>
      <% @userList = User.where(:email => user.email) %>
      <% @userList.each do |match|%>
        <% if !match.email.blank? %>
          <p> Position: <%= match.admin %> <p>
        <% end %>
        <% if match.admin %>
          <p> You currently have <%= match.member_points %> points.</p>
        <% else %>
          <p> You have permission to add users.</p>
        <% end %>
      <% end %>
  <% end %>
</div>

<% if !flash[:notice].blank? %>
  <div class="notice">
    <%= flash[:notice] %>
  </div>
<% end %>
<% @current_user = Admin.where(id: 1) %>
<% @current_user.each do |user| %>
  <% @userList = User.where(:email => user.email) %>
  <% @userList.each do |match|%>
<% if !match.admin %>
  <div class='button_center'>
    <%= button_to "Points Breakdown", attendance_path(match), method: :get, class: 'button' %>
  </div>
<% else %>
    <div class="users index">

    <h1>Members</h1>
    <table class="listing" summary="User list" cellspacing="10" style="width:1800px">
      <tr class="header"  style="text-align:left">
        <th>NetID</th>
        <th>Email</th>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Position</th>
        <th>Points</th>
      </tr>
      <% @users.each do |user| %>
      <tr  style="text-align:left">
        <td><%= user.username %></td>
        <td><%= user.email %></td>
        <td><%= user.firstname %></td>
        <td><%= user.lastname %></td>
        <td><%= user.admin %></td>
        <td><%= user.member_points %></td>
        <% @userList.each do |match|%>
        <td class="actions">
            <%= button_to "Points Breakdown", attendance_path(match), method: :get, class: 'table_button' %>
        </td>
        <% end %>
      </tr>
      <% end %>
    </table>
  <% end %>
  <% end %>
  <% end %>
