<!DOCTYPE html>
<html>
  <head>
    <title>GoogleOauthDevise</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <style>
        .flash__alert   { color: red; }
        .flash__notice  { color: blue; }
        .flash__success { color: green; }
    </style>
  </head>

  <body>
    <%= image_tag("AWICS_logo.png", width: '200px') %>
    <% if !current_page?new_admin_session_path %>
      <ul>
          <li><%= link_to "Members List", users_path %></li>
          <% @current_user = Admin.where(id: 1) %>
          <% @current_user.each do |user| %>
            <% @userList = User.where(:email => user.email) %>
            <% @userList.each do |match|%>
            <% if match.position_id == 'admin' %>
              <li><%= link_to "Add New Member", new_user_path %></li>
            <% end %>
          <% end %>
          <% end %>
          <li><%= link_to "Sign Out", destroy_admin_session_path %></li>
      </ul>
    <% end %>
    <% if flash.any? %>
      <% flash.each do |key, value| -%>
        <section class="flash flash__<%= key %>"><%= value.html_safe %></section>
      <% end -%>
    <% end %>

    <%= yield %>
  </body>
</html>

<%# <!DOCTYPE html> %>
<%# <html> %>
  <%# <head> %>
    <%# <title>AwicsMpt</title> %>
    <%# <%= csrf_meta_tags %>
    <%# <%= csp_meta_tag %>

    <%# <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%# <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  <%# </head> %>

  <%# <body> %>
    <%# <%= yield %>
  <%# </body> %>
<%# </html> %>
